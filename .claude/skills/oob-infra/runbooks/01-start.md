## Overview
Start interactsh client session.

## Steps
1. Run `start-client`
2. Check `session.json` has `URL`

## Verification
- Session shows active status with callback URL
- `session.json` has valid `token` and `server` fields