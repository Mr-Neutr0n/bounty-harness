## Overview
Test workflow step skipping, repeating, reordering.

## Steps
1. Run `test-skip-step`
2. Run `test-repeat-step`
3. Run `test-reorder-steps`
4. Check `transition_tests.jsonl`

## Verification
- Each test records invariant checked and result
- Violations flagged with affected workflow + step