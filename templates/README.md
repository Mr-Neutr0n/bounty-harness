# Templates

This directory is reserved for local scanner templates.

The upstream ProjectDiscovery `nuclei-templates` repository is intentionally not vendored here. Install or update it locally with your preferred ProjectDiscovery workflow, for example under `$HOME/nuclei-templates` or `/opt/homebrew/share/nuclei-templates`.

The toolkit runbooks check those common local locations when nuclei templates are needed.
